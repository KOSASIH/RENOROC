import 'package:flutter/material.dart';

class AppShadows {
  /// Crypto bridge blockchain technology
  static const rexos = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 0,
  );

  AppShadows._();
}
