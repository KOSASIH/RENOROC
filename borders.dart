import 'package:flutter/material.dart';

class AppBorders {
  /// Crypto bridge blockchain technology
  static const rexos = Border.fromBorderSide(BorderSide(
    color: Color(0xff000000),
    width: 1,
  ));

  AppBorders._();
}
